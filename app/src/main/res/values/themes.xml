<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with Soft Modern Colors -->
    <style name="Theme.ToDoListApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_soft</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent_teal</item>
        <item name="colorSecondaryVariant">@color/accent_teal</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background and Surface colors -->
        <item name="android:windowBackground">@color/bg_light</item>
        <item name="colorSurface">@color/surface_white</item>
        <item name="colorOnSurface">@color/text_main</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_soft</item>

        <!-- UI Elements Customization -->
        <item name="editTextStyle">@style/CustomEditTextStyle</item>
    </style>

    <style name="CustomEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:backgroundTint">@color/primary_soft</item>
        <item name="android:textColor">@color/text_main</item>
        <item name="android:textColorHint">@color/text_hint</item>
    </style>
</resources>